import styled from 'styled-components';

const Root = styled.div`
  display: flex;
  align-items: center;
  gap: 8px;
`;

const NumberWrapper = styled.div`
  width: 30px;
  display: flex;
  justify-content: center;
  margin-right: 10px;
`;

export { Root, NumberWrapper };
