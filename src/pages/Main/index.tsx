import React from 'react';
import MenuContainer from 'systems/MenuContainer';
import { Root } from './styled';

const Main = () => {
  return (
    <Root>
      <MenuContainer />
    </Root>
  );
};

export default Main;
